import React from 'react';
import './LaunchFilters.scss';

const LaunchFilters: React.FC = () => (
  <div className="LaunchFilters" data-testid="LaunchFilters">
    LaunchFilters Component
  </div>
);

export default LaunchFilters;
